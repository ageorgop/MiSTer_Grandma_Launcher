# Local builds use `cross` tool for ARM cross-compilation.
# `cross build --target armv7-unknown-linux-musleabihf --release`
# No linker override needed - cross provides the musl toolchain in Docker.
